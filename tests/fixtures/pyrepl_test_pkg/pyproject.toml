[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "pyrepl_test_pkg"
version = "1.0.0"
description = "Minimal package for sphinx-pyrepl-web wheel fixture tests"
